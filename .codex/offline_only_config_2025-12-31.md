# Offline-only experiment config

## Summary
- Added `.configs/offline_only.toml` for running VIN with offline cache only.
- Config uses offline cache for both train/val, disables cache appending, and enables fast_dev_run.

## Files
- `.configs/offline_only.toml`
