# VIN NBV — Voxel Extent vs Semidense Bounds Note

## Change
- Added a short “debug note” under `voxel_extent` in `docs/contents/impl/vin_nbv.qmd` explaining why EVL’s voxel grid appears much smaller than the semidense cloud (fixed local volume anchored at `voxel/selectT`).
- Documented the active config source (`external/efm3d/efm3d/config/evl_inf_desktop.yaml`) and linked the raw file for reference.

## Command
- `make context-qmd-tree`
