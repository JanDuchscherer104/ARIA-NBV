from ..views.candidate_generation import CandidateSamplingResult, CandidateViewGenerator

CandidateViewGenerator()
CandidateSamplingResult()

# TODO: implement free functions to visualize the different stages of the CanidateGneration: i.e. _rule_shell_sampling, _sample_directions, _rule_min_distance_to_mesh, _rule_path_collision, _rule_free_space
# TODO: should be a pipeline: come up with a plan on how we can do this before continuing!
