diff --git a/oracle_rri/oracle_rri/utils/frames.py b/oracle_rri/oracle_rri/utils/frames.py
index ec356ef..xxxxxxx 100644
--- a/oracle_rri/oracle_rri/utils/frames.py
+++ b/oracle_rri/oracle_rri/utils/frames.py
@@ -1,6 +1,7 @@
 from __future__ import annotations

 import math
+import textwrap
 from dataclasses import dataclass
 from typing import Iterable

@@ -142,37 +143,28 @@ def view_axes_from_points(
     return torch.stack([left, up, fwd], dim=-1)  # [..., 3, 3]


 def pose_for_display(
     t_world_cam: PoseTW,
     gravity_w: torch.Tensor | None = None,
     *,
     align_gravity: bool = True,
 ) -> PoseTW:
-    """Return a display-friendly PoseTW with gravity-up and UI roll/yaw corrections.
-
-    Steps (mirrors data.plotting):
-    1) Gravity-align so world +Z is up (undo VIO gravity tilt).
-    2) Convert Aria LUF camera axes to a display frame (180° about +Z).
-    3) Apply fixed rotations:
-       - roll -90° about camera z to match rotated image display,
-       - yaw -90° about camera y to align forward/right/down with viewer.
-    This keeps frusta upright in Plotly and avoids roll (x-axis) tilt.
-    """
-    # 1) Optional gravity alignment (kept for backwards compat with older callers).
-    if align_gravity and gravity_w is not None:
-        t_aligned = gravity_align_pose(t_world_cam, gravity_w)
-    else:
-        t_aligned = t_world_cam
-
-    r_base = t_aligned.R()
-    t_base = t_aligned.t()
-
-    # 2–3) Fixed SO(3) corrections used by the Streamlit/Plotly viewers.
-    # NOTE: these were historically tuned by trial-and-error and are the
-    # source of the recent frustum/axis confusion.  We keep the function
-    # for API compatibility but callers that care about geometry should
-    # *not* rely on these display rotations.
-    cz, sz = (
-        torch.cos(torch.tensor(-math.pi / 2, device=t_aligned.device, dtype=t_aligned.dtype)),
-        torch.sin(torch.tensor(-math.pi / 2, device=t_aligned.device, dtype=t_aligned.dtype)),
-    )
-    cy, sy = (
-        torch.cos(torch.tensor(-math.pi / 2, device=t_aligned.device, dtype=t_aligned.dtype)),
-        torch.sin(torch.tensor(-math.pi / 2, device=t_aligned.device, dtype=t_aligned.dtype)),
-    )
-    r_z = torch.tensor(
-        [[cz, -sz, 0.0], [sz, cz, 0.0], [0.0, 0.0, 1.0]],
-        device=t_aligned.device,
-        dtype=t_aligned.dtype,
-    )
-    r_y = torch.tensor(
-        [[cy, 0.0, sy], [0.0, 1.0, 0.0], [-sy, 0.0, cy]],
-        device=t_aligned.device,
-        dtype=t_aligned.dtype,
-    )
-    # First yaw about camera +Y (viewer alignment), then roll about camera +Z (image rotation).
-    # NOTE: order matters – we want a pure SO(3) correction that stays compatible with
-    # efm3d/ATEK visualisation.
-    r_display = (r_z @ r_y) @ r_base
-    t_display = t_base
-
-    return PoseTW.from_Rt(r_display, t_display)
+    """Return a *display* pose without changing the underlying geometry.
+
+    Historically this function applied a stack of ad-hoc roll/yaw
+    corrections in an attempt to make Plotly's default 3D camera match
+    the RGB image orientation.  Those extra rotations turned out to be
+    brittle and were the root cause of the frustum/axis misalignment
+    you observed in the mesh+semidense viewers.
+
+    To make the behaviour predictable again we now treat this as a
+    **no-op** on the SE(3) pose:
+
+        - The input pose ``t_world_cam`` is assumed to be a proper Aria
+          ``T_world,cam`` in the LUF convention (X-left, Y-up, Z-forward).
+        - The same pose is returned unchanged; no additional rotations
+          are applied here.
+        - Display code (e.g. Plotly layout / Streamlit camera controls)
+          is responsible for any viewer-specific adjustments.
+
+    The function is kept only for backwards compatibility with older
+    callers that expect a :class:`PoseTW` to be returned.
+    """
+    # NOTE: we intentionally ignore ``gravity_w`` and ``align_gravity`` here.
+    # All gravity alignment for plotting should happen explicitly via
+    # the world frame (EFM/Aria already provides gravity in -Z).
+    return t_world_cam


 def world_from_rig_camera_pose(
     t_world_rig: PoseTW,
     camera: CameraTW,
     frame_idx: int,
diff --git a/oracle_rri/oracle_rri/data/plotting.py b/oracle_rri/oracle_rri/data/plotting.py
index 1234567..89abcde 100644
--- a/oracle_rri/oracle_rri/data/plotting.py
+++ b/oracle_rri/oracle_rri/data/plotting.py
@@ -22,7 +22,7 @@ import plotly.graph_objects as go
 from efm3d.aria.camera import CameraTW
 from efm3d.aria.pose import PoseTW

-from ..utils.frames import pose_for_display, world_from_rig_camera_pose
+from ..utils.frames import world_from_rig_camera_pose


 # ---------------------------------------------------------------------------
@@ -70,18 +70,25 @@ def _frustum_segments(
     return segments


 def _aligned_pose_world_cam(
     t_world_rig: PoseTW,
     camera: CameraTW,
     frame_idx: int,
 ) -> PoseTW:
-    """Helper: world←camera pose with gravity/alignment tweaks for display."""
-    t_world_cam = world_from_rig_camera_pose(t_world_rig, camera, frame_idx)
-    # Historically we applied extra roll/yaw corrections here via
-    # :func:`pose_for_display`.  Those have been the source of several
-    # frustum/axis bugs; keep plotting in the canonical frame instead.
-    return pose_for_display(t_world_cam, gravity_w=None, align_gravity=False)
+    """Return the true Aria world←camera pose for plotting.
+
+    We purposely **do not** apply any additional "display" rotations
+    here.  The returned pose:
+
+      * lives in the VIO world frame (gravity in -Z),
+      * uses the Aria/EFM3D camera convention (LUF: X-left, Y-up, Z-forward),
+      * is the same SE(3) that the renderers and depth debugger use.
+
+    This keeps the camera axes and frustum geometry consistent with the
+    mesh and semidense points.  Any viewer-specific tweaks (e.g. Plotly
+    camera settings) should be handled at the layout level instead of
+    by rotating the physical pose here.
+    """
+    return world_from_rig_camera_pose(t_world_rig, camera, frame_idx)


 class SnippetPlotBuilder:
@@ -167,7 +174,7 @@ class SnippetPlotBuilder:
         cam_pose_world = _aligned_pose_world_cam(trajectory, camera, frame_idx)
         segments = _frustum_segments(camera, cam_pose_world, color)
         for seg in segments:
-            self.fig.add_trace(seg)
+            self.fig.add_trace(seg)

     def add_camera_axes(self, cam_center: np.ndarray, cam_axes: np.ndarray) -> None:
         """Add LUF camera axes at the given centre.
@@ -176,7 +183,9 @@ class SnippetPlotBuilder:
         x, y, z = cam_axes
-        # We assume cam_axes are already in the same world frame as the mesh/points.
+        # NOTE: ``cam_axes`` are expected to come from the same Aria/EFM3D
+        # world frame as the mesh and semidense points (i.e. after
+        # :func:`world_from_rig_camera_pose`), with LUF columns.
         self.fig.add_trace(
             go.Scatter3d(
                 x=[cam_center[0], cam_center[0] + x[0]],
