---
description: Validate agent memory and skills (make check-agent-memory).
allowed-tools: Bash(make check-agent-memory)
---

Run `make check-agent-memory` and report any failures with the exact line
references. If the run is clean, say so in one sentence.
