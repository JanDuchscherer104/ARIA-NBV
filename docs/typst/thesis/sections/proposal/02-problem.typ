#import "../../../shared/macros.typ": *

= Problem Statement

The core problem is to predict and plan with #RRI signals for egocentric indoor scenes in #ASE. The proposal should make the target setting, candidate action space, reconstruction-quality metric, and evaluation budget explicit before expanding to non-myopic planning.
