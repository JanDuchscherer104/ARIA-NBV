# Pose-Generation Diagrams

This directory contains public Mermaid sources and rendered assets for
target-conditioned target selection, candidate sampling, mixture provenance, and
rollout branch selection.

Render sources with:

```bash
tools/mermaid/scripts/render_mermaid.sh <file.mmd> <file.svg>
```
