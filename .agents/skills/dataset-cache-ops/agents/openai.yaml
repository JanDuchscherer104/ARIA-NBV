interface:
  display_name: "Dataset Cache Ops"
  short_description: "ASE data, VIN offline stores, and dataset-version updates"
  default_prompt: "Use $dataset-cache-ops for ARIA-NBV ASE download, ATEK shard, mesh, VIN offline store, dataset-version update, split manifest, and data smoke-check work."
