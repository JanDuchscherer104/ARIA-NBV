# ARIA-NBV TODOs

[[todo]]
id = "todo-001"
title = "Classify and prune docs/"
description = "Inventory docs/ and triage pages into public thesis narrative, paper/slides source, generated context, internal agent material, or archive."
priority = "high"
status = "todo"
labels = ["docs", "hygiene"]
issue_ids = ["issue-003"]
loc_min = 200
loc_expected = 700
loc_max = 1600
context = ["Current docs expose roadmap, TODOs, ideas, generated agent scaffold mirrors, literature notes, implementation guides, and scratchpad-like pages in the same public tree.", "docs/AGENTS.md says docs/typst/paper/main.typ is ground truth and Quarto should not introduce competing top-level narratives."]
implementation_notes = ["Start with scripts/nbv_qmd_outline.sh --compact and docs/_quarto.yml navigation instead of broad manual browsing.", "Keep source pages until there is an explicit archive/move decision; first remove public navigation pressure and stale links.", "Do not delete history records under .agents/memory/history/."]
acceptance = ["Each docs subtree has an explicit role: public, paper, slides, generated, internal mirror, or archive.", "Public nav no longer surfaces internal agent scaffolding or stale work-management pages as primary project content.", "Stale typo links such as coral_intergarion and hesita are either renamed or intentionally documented."]
verification = ["scripts/nbv_qmd_outline.sh --compact", "cd docs && quarto render index.qmd contents/thesis/roadmap.qmd contents/thesis/questions.qmd contents/archive/index.qmd", "rg -n \"Seminar|hesita|coral_intergarion|old NBV repo URL\" README.md docs"]

[[todo]]
id = "todo-002"
title = "Add SETUP.md"
description = "Create a dedicated SETUP.md for environment, CUDA, dataset access, cache paths, and first-run smoke commands."
priority = "high"
status = "todo"
labels = ["scaffold", "dev-ergonomics"]
issue_ids = ["issue-005"]
loc_min = 80
loc_expected = 180
loc_max = 360
context = ["README.md should stay concise, while setup details need a stable place for uv sync, Project Aria/ATEK/EFM dependencies, cache roots, and offline-only smoke tests.", ".agents/references/operator_quick_reference.md can be used for machine-specific recovery details, but shared setup docs must stay portable."]
implementation_notes = ["Create root SETUP.md or a docs/contents/setup.qmd-aligned root pointer without duplicating host-specific paths.", "Include uv sync --all-extras from aria_nbv/, VIN offline-store expectations, and Streamlit/VIN summary smoke commands.", "Link from README.md and docs/index.qmd."]
acceptance = ["A new contributor can find environment setup, cache prerequisites, and the first smoke commands from the README.", "No shared setup text contains operator-specific interpreter paths."]
verification = ["rg -n \"host-local home path|miniforge\" README.md SETUP.md docs/contents/setup.qmd", "cd docs && quarto render index.qmd contents/setup.qmd"]

[[todo]]
id = "todo-003"
title = "Implement oracle-rri-contracts skill"
description = "Create a skill to protect technical layers: poses, cameras, RRI binning semantics, and oracle reporting."
priority = "medium"
status = "todo"
labels = ["agent", "research"]
issue_ids = ["issue-007"]
loc_min = 80
loc_expected = 180
loc_max = 360
context = ["RRI semantics are contract changes, not local refactors.", "aria_nbv/aria_nbv/rri_metrics/AGENTS.md already defines the boundary for oracle labels, binning, ordinal loss, and reported metric meaning."]
implementation_notes = ["Base the skill on rri_metrics/AGENTS.md, GOTCHAS.md frame notes, and docs/contents/impl/rri_computation.qmd.", "Require tensor shapes, frames, units, and metric interpretation in any public RRI docstrings."]
acceptance = ["The skill routes RRI/oracle tasks to the right docs, tests, and gotchas.", "No copied wrong-repo text remains."]
verification = ["rg -n \"PRML|litkg-rs|Rust\" .agents/skills/oracle-rri-contracts .agents/skills || true", "make check-agent-memory"]

[[todo]]
id = "todo-004"
title = "Deferred vin-training-eval skill"
description = "Create a deferred skill for VIN scorer experiments, offline-store training, diagnostics, and W&B/Optuna logging once VIN training work resumes."
priority = "medium"
status = "todo"
labels = ["agent", "research"]
issue_ids = ["issue-007"]
loc_min = 80
loc_expected = 180
loc_max = 360
context = ["M2 requires a reproducible one-step VIN baseline, calibration/ranking plots, and a fixed ablation matrix.", "The current app and Lightning stack already expose VIN diagnostics, summary, W&B, and Optuna surfaces.", "The scaffold cleanup intentionally adds only diagnose-aria and plan-grill now; VIN training/eval should remain backlog until active VIN experiment work resumes."]
implementation_notes = ["Route to aria_nbv/aria_nbv/vin/AGENTS.md, app VIN diagnostics helpers, Lightning CLI, and docs/contents/impl/vin_nbv.qmd.", "Capture validation gotcha: trainer_config.enable_validation must be true for validation to run.", "Use the final name vin-training-eval unless a later plan selects a narrower name."]
acceptance = ["The skill gives a compact experiment workflow for offline_only.toml, nbv-summary, targeted VIN tests, and diagnostics interpretation.", "The skill names current ARIA-NBV surfaces only."]
verification = ["make check-agent-memory", "rg -n \"PRML|litkg-rs|Rust\" .agents/skills/vin-training-diagnostics .agents/skills || true"]

[[todo]]
id = "todo-005"
title = "Build entity-aware oracle RRI"
description = "Implement GT-OBB-cropped oracle RRI as defined in the thesis system."
priority = "high"
status = "todo"
labels = ["research", "core"]
issue_ids = ["issue-007"]
loc_min = 350
loc_expected = 900
loc_max = 1800
context = ["Thesis RQ3/RQ4 require target-specific RRI before target-conditioned VIN can be evaluated.", "GOTCHAS.md notes EVL OBB outputs are not batch-collatable yet, so first runs may need batch_size=None or OBB outputs disabled outside the target-aware path."]
implementation_notes = ["Start with GT OBBs and a small trusted subset; do not begin with predicted OBB realism ablations.", "Keep target mesh/point support, invalid target reasons, and scene-level-vs-target-level diagnostics explicit.", "Use existing PoseTW and CameraTW contracts; do not introduce raw matrix interfaces."]
acceptance = ["GT-OBB cropped RRI runs on a small subset with side-by-side target RRI and scene RRI diagnostics.", "Unsupported targets have explicit invalid reasons rather than being encoded as low RRI.", "Docs and tests describe target frames, crop margin, units, and failure modes."]
verification = ["cd aria_nbv && uv run pytest tests/rri_metrics tests/data_handling/test_vin_offline_store.py", "cd docs && quarto render contents/thesis/questions.qmd contents/thesis/roadmap.qmd"]

[[todo]]
id = "todo-006"
title = "Aria-NBV Gym simulator setup"
description = "Build a Gym-style simulator using PyTorch3D and ASE GT meshes for discrete rollouts after M1/M2 contracts are stable."
priority = "medium"
status = "todo"
labels = ["research", "sim"]
issue_ids = ["issue-007"]
loc_min = 300
loc_expected = 900
loc_max = 1800
context = ["The roadmap places multi-step rollout comparison in August and the value/RL gate after rollout evidence, so simulator work should not outrun data/RRI contract stabilization.", "Current canonical state frames full continuous RL as a stretch direction rather than the thesis core."]
implementation_notes = ["Begin with discrete candidate shell rollouts and hard masks; avoid full continuous control until the gate evidence exists.", "Keep oracle reward, VIN surrogate reward, state modalities, and invalid action handling separated."]
acceptance = ["Simulator API can replay bounded discrete rollouts with cumulative target or scene RRI accounting.", "Invalid actions are masked or reported with reasons, not silently treated as low-RRI candidates."]
verification = ["cd aria_nbv && uv run pytest tests/pose_generation tests/rl", "make context-contracts"]

[[todo]]
id = "todo-007"
title = "M1 data/cache/oracle contract audit and stabilization"
description = "Freeze the offline store, split, frame/CW90, and oracle RRI contract before new target-conditioned model work."
priority = "high"
status = "todo"
labels = ["data", "rri", "contracts", "thesis"]
issue_ids = ["issue-007"]
loc_min = 160
loc_expected = 420
loc_max = 1000
context = ["Roadmap M1 exit criteria require stable offline store and RRI contracts, documented frame/CW90 risks, and measured oracle throughput.", "data_handling/AGENTS.md says readers should validate strictly and fail with rebuild guidance rather than mutate derived artifacts.", "rri_metrics/AGENTS.md says metric semantics, binning definitions, and reported metric meanings are contract changes."]
implementation_notes = ["Run make context-contracts and targeted data/RRI tests first; record failures in issues/todos before changing package semantics.", "Confirm offline_only.toml uses apply_cw90_correction=false and that pose/camera frame alignment tests cover this setting.", "Collect oracle throughput on representative snippets only after correctness checks pass."]
acceptance = ["Targeted data_handling and rri_metrics tests pass or DB records identify exact blockers.", "Frame/CW90 contract is documented in active docs or gotchas.", "A throughput command/report path is identified for representative snippets."]
verification = ["make context-contracts", "cd aria_nbv && uv run pytest tests/data_handling/test_vin_offline_store.py tests/data_handling/test_public_api_contract.py", "cd aria_nbv && uv run pytest tests/rri_metrics"]

[[todo]]
id = "todo-008"
title = "Validate offline_only.toml VIN offline store in VIN diagnostics"
description = "Smoke the available VIN offline store through the Streamlit VIN diagnostics and summary path."
priority = "high"
status = "todo"
labels = ["m0", "vin", "diagnostics", "offline-store"]
issue_ids = ["issue-007", "issue-014"]
loc_min = 40
loc_expected = 120
loc_max = 260
context = ["Roadmap M0 requires validating that offline_only.toml can load the available VIN offline store into the VIN diagnostics page.", "aria_nbv/tests/vin/test_vin_utils.py already checks that the diagnostics config preserves the offline source and disables W&B.", "2026-04-29 smoke reached VinOfflineStoreReader and failed on missing `.data/offline_cache/vin_offline/manifest.json`; no manifest/sample_index exists under the repo."]
implementation_notes = ["Prefer nbv-summary as a non-interactive smoke before manual Streamlit inspection.", "Use `--config-path offline_only.toml`; bare config names resolve under `.configs/`.", "Do not change offline store payloads to silence failures; materialize the store or point the config at an existing immutable store.", "Keep apply_cw90_correction=false unless candidate poses, p3d_cameras, and reference pose are rotated in lockstep."]
acceptance = ["nbv-summary or an equivalent diagnostics helper loads at least one train/val batch from offline_only.toml.", "Streamlit VIN diagnostics can point at offline_only.toml without falling back to online data.", "Any failure has an issue with exact command, traceback summary, and affected contract."]
verification = ["cd aria_nbv && uv run nbv-summary --config-path offline_only.toml", "cd aria_nbv && uv run pytest tests/vin/test_vin_utils.py"]

[[todo]]
id = "todo-011"
title = "Add root CI, pre-commit, and make ci"
description = "Port the PRML-style local verification contract into ARIA-NBV with root make targets, pre-commit parity, and a general GitHub Actions CI workflow."
priority = "high"
status = "todo"
labels = ["ci", "dev-ergonomics", "scaffold"]
issue_ids = ["issue-004"]
loc_min = 120
loc_expected = 260
loc_max = 620
context = ["The PRML cross-review identifies root make ci, lint/test/docs checks, pre-commit, and GitHub CI as scaffold discipline that ARIA-NBV still lacks.", "ARIA-NBV currently has a Quarto publish workflow but no root CI workflow or .pre-commit-config.yaml.", "The repo already has targeted Makefile checks for context, Typst, KG, and agent memory that can be composed into a lightweight default gate."]
implementation_notes = ["Add make fmt-check, lint, test, docs-check, agent-check, and ci targets that delegate to uv under aria_nbv/ and existing docs/memory checks.", "Keep GPU, full dataset, and long experiment tests opt-in; the default CI should be reliable on a clean CPU runner.", "Add .pre-commit-config.yaml for merge-conflict checks, TOML/YAML/basic whitespace checks, Ruff format check, and Ruff lint check where feasible."]
acceptance = ["make ci exists and is documented as the local verification gate.", ".github/workflows/ci.yml runs the lightweight source/docs/agent-memory checks on PR/push.", ".pre-commit-config.yaml exists and its core checks match CI rather than adding unrelated policy."]
verification = ["make ci", "pre-commit run --all-files", "find .github/workflows -maxdepth 1 -type f -name 'ci.yml' -print"]

[[todo]]
id = "todo-012"
title = "Add GitHub issue and PR governance templates"
description = "Create the repository collaboration scaffold recommended by the PRML cross-review: labels, milestones, issue templates, PR template, and draft-PR disposition guidance."
priority = "medium"
status = "todo"
labels = ["github", "workflow", "scaffold"]
issue_ids = ["issue-011"]
loc_min = 80
loc_expected = 180
loc_max = 420
context = ["The PRML comparison called out missing milestones, labels, project-board views, issue templates, PR template, and explicit draft-PR disposition.", "The local agents DB now owns active backlog, so GitHub templates should route work into reproducible acceptance and verification rather than duplicating all TOML records."]
implementation_notes = ["Add .github/ISSUE_TEMPLATE entries for bug, research task, docs task, and refactor work.", "Add .github/pull_request_template.md with summary, affected contracts, verification, docs/memory updates, and limitations.", "Document label/milestone taxonomy in a repo-local reference if it cannot be created directly through GitHub in the current task."]
acceptance = ["New GitHub issues ask for affected files, contract impact, acceptance criteria, and verification.", "PR template is reviewer-first and asks for docs/memory updates when behavior changes.", "Open draft PR triage has a repeatable local checklist or issue template."]
verification = ["find .github -maxdepth 3 -type f | sort", "rg -n \"Acceptance|Verification|Affected contracts|Docs|Memory\" .github"]

[[todo]]
id = "todo-018"
title = "Create experiment registry and final claim matrix"
description = "Add a reproducibility surface that maps final figures, tables, ablations, and thesis claims to run IDs, configs, cache hashes, split manifests, metrics, and source artifacts."
priority = "medium"
status = "todo"
labels = ["experiments", "thesis", "reproducibility"]
issue_ids = ["issue-013"]
loc_min = 120
loc_expected = 260
loc_max = 620
context = ["Roadmap M7/M8 require final tables, figures, failure cases, and reproducible configs.", "The PRML-style issue register recommended a final experiment registry, run-report generator, claim matrix, and release checklist."]
implementation_notes = ["Start with a small docs or .agents-owned registry schema before building automation.", "Record run ID, config path/hash, cache/store ID, split manifest, candidate profile, seed, checkpoint, metric table, and figure output.", "Keep final claims explicit: do not claim continuous RL, real-device deployment, or VLM/global planning without evidence."]
acceptance = ["There is a canonical place to trace final experiment artifacts to source inputs.", "At least one smoke/final-row example is represented.", "The registry can be referenced from paper/slides without copying unverified run notes."]
verification = ["rg -n \"claim matrix|run_id|config_hash|cache_hash|split\" docs .agents", "make check-agent-memory"]
