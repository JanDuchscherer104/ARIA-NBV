[metadata]
name = openpoints-shim
version = 0.1.5
description = NBV shim that builds and exposes OpenPoints ops.

[options]
packages = find:
include_package_data = True
zip_safe = False
python_requires = >=3.11
py_modules = sitecustomize

[options.packages.find]
where = .
